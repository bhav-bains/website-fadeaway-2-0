---
import { Image } from "astro:assets";
import fadeawayLogo from "../assets/fadeaway-logo-2.png";

const { pathname } = Astro.url;
const normalizedPath = pathname.replace(/\/$/, "");

const navLinks = [
  { label: 'Sports Playbook', href: '/sports' },
  { label: 'Wellness Playbook', href: '/wellness' },
];

const socialLinks = [
  {
    name: 'Twitter',
    href: '#',
    icon: `<svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M23 3a10.9 10.9 0 01-3.14 1.53 4.48 4.48 0 00-7.86 3v1A10.66 10.66 0 013 4s-4 9 5 13a11.64 11.64 0 01-7 2s9 5 20 5a9.5 9.5 0 00-9-5.5c4.75 2.25 7-7 7-7" /></svg>`
  },
  {
    name: 'LinkedIn',
    href: '#',
    icon: `<svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M16 8a6 6 0 016 6v7h-4v-7a2 2 0 00-2-2 2 2 0 00-2 2v7h-4v-7a6 6 0 016-6zM2 9h4v12H2z" /><circle cx="4" cy="4" r="2" /></svg>`
  },
  {
    name: 'Instagram',
    href: '#',
    icon: `<svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><rect width="20" height="20" x="2" y="2" rx="5" ry="5" /><path d="M16 11.37A4 4 0 1112.63 8 4 4 0 0116 11.37zm1.5-4.87h.01" /></svg>`
  },
];
---

<header id="main-header" class="z-[100] transition-all duration-300 absolute top-0 left-0 right-0 py-3 px-4 md:px-6">
  <div class="container-max flex items-center justify-between md:hidden">
    <a href="/sports" class="z-50">
      <Image src={fadeawayLogo} alt="Fadeaway Creatives" class="h-8 w-auto" />
    </a>
    
    <!-- Mobile Menu Button -->
    <button
      id="menu-toggle"
      aria-label="Toggle Menu"
      class="hover:bg-white/5 rounded-lg transition-all duration-300 backdrop-blur-md bg-black/30 border border-white/10 p-1 text-white"
    >
      <svg id="menu-icon" class="w-4 h-4 fill-none stroke-current" viewBox="0 0 24 24">
        <path
          class="menu-path-open"
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"
        />
        <path
          class="menu-path-close hidden"
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M6 18L18 6M6 6l12 12"
        />
      </svg>
    </button>
  </div>

  <!-- Desktop Navigation -->
  <div class="hidden md:block container-max max-w-5xl mx-auto">
    <div id="desktop-nav-container" class="backdrop-blur-md bg-black/30 border border-white/10 shadow-lg transition-all duration-300 flex items-center justify-between px-6 rounded-xl py-1.5">
      <a href="/sports" class="flex-shrink-0">
        <Image src={fadeawayLogo} alt="Fadeaway Creatives" class="h-10 w-auto" />
      </a>

      <nav class="flex items-center justify-center gap-6">
        <div class="flex flex-row items-center gap-2">
          {navLinks.map((link) => {
            const isActive = normalizedPath === link.href;
            return (
              <a
                href={link.href}
                class={`transition-all duration-300 font-medium text-sm px-3 py-1 relative ${
                  isActive 
                  ? "text-white after:absolute after:bottom-0 after:left-3 after:right-3 after:h-0.5 after:bg-fadeaway-orange" 
                  : "text-neutral-400 hover:text-white"
                }`}
              >
                {link.label}
              </a>
            );
          })}
        </div>
      </nav>

      <a
        href="/contact"
        class="bg-fadeaway-orange/10 border border-fadeaway-orange/50 text-fadeaway-orange hover:bg-fadeaway-orange hover:text-black transition-all duration-300 font-bold text-xs px-4 py-1.5 rounded-lg uppercase tracking-wider shadow-[0_0_10px_rgba(249,115,22,0.15)]"
      >
        See Demo
      </a>
    </div>
  </div>

  <!-- Mobile Navigation Menu -->
  <nav id="mobile-menu" class="hidden md:hidden absolute top-full left-0 right-0 h-[calc(100vh-60px)] backdrop-blur-sm bg-black/40 transition-all duration-300">
    <div class="m-4 rounded-3xl backdrop-blur-2xl bg-[#1a1a1c]/90 border border-white/10 shadow-[0_0_40px_rgba(0,0,0,0.5)] flex flex-col items-stretch gap-4 p-6 py-8 w-full max-w-sm mx-auto mt-4">
      {navLinks.map((link) => {
        const isActive = normalizedPath === link.href;
        return (
          <a
            href={link.href}
            class={`mobile-nav-link transition-all duration-300 flex items-center justify-between font-outfit font-medium text-[17px] px-5 py-4 rounded-2xl w-full ${
              isActive 
              ? "bg-fadeaway-orange/20 border border-fadeaway-orange/50 text-white shadow-inner" 
              : "bg-white/5 border border-white/10 text-neutral-300 hover:bg-white/10 hover:text-white"
            }`}
          >
            <span>{link.label}</span>
            <svg class={`w-5 h-5 ${isActive ? 'text-fadeaway-orange shadow-sm' : 'text-neutral-500'}`} fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        );
      })}
      
      <div class="h-px bg-white/10 my-2 w-full"></div>

      <a
        href="/contact"
        class="mobile-nav-link bg-fadeaway-orange hover:bg-orange-500 text-black transition-all duration-300 font-bold text-[15px] px-6 py-4 rounded-2xl uppercase tracking-wider w-full text-center shadow-[0_0_20px_rgba(249,115,22,0.3)] mt-2"
      >
        See Demo
      </a>
    </div>
  </nav>
</header>

<script>
  const menuToggle = document.getElementById('menu-toggle');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuPathOpen = document.querySelector('.menu-path-open');
  const menuPathClose = document.querySelector('.menu-path-close');
  const mobileNavLinks = document.querySelectorAll('.mobile-nav-link');

  // Handle Mobile Menu Toggle
  function toggleMenu() {
    const isExpanded = !mobileMenu?.classList.contains('hidden');
    if (mobileMenu && menuPathOpen && menuPathClose) {
      if (isExpanded) {
        mobileMenu.classList.add('hidden');
        mobileMenu.classList.remove('flex', 'flex-col');
        menuPathOpen.classList.remove('hidden');
        menuPathClose.classList.add('hidden');
      } else {
        mobileMenu.classList.remove('hidden');
        mobileMenu.classList.add('flex', 'flex-col');
        menuPathOpen.classList.add('hidden');
        menuPathClose.classList.remove('hidden');
      }
    }
  }

  // Close menu on link click
  mobileNavLinks.forEach(link => {
    link.addEventListener('click', () => {
      mobileMenu?.classList.add('hidden');
      mobileMenu?.classList.remove('flex', 'flex-col');
      menuPathOpen?.classList.remove('hidden');
      menuPathClose?.classList.add('hidden');
    });
  });

  menuToggle?.addEventListener('click', toggleMenu);
</script>
